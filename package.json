{
  "name": "root",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/dicebear",
      "packages/@dicebear/*",
      "apps/*"
    ]
  },
  "scripts": {
    "build": "lerna run build",
    "test": "lerna run test",
    "prepare-publish": "lerna version --force-publish --exact",
    "tool": "cd ../.. && npm run tool"
  },
  "devDependencies": {
    "lerna": "^8.1.2"
  }
}
